(import "prelude")

(defun size (xs)
  (if (= xs ()) 0 (+ 1 (size (tail xs)))))

(defun empty? (xs) (= (size xs) 0))

(defun find (xs f)
  (tail-rec ((xs xs))
    (if (empty? xs) nil
      (let ((x (head xs))) 
        (if (f x) x (recur (tail xs)))))))